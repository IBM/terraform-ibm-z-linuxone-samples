# Subtitute these variables and use them for your Terraform backend config credential so AAP can store and retrieve Terraform state file
hostname = "YOUR_TERRAFORM_ENTERPRISE_HOSTNAME" 
organization = "YOUR_ORGANIZATION_NAME" 
token = "YOUR-TERRAFORM-SERVER-API-TOKEN"
workspaces {name = "YOUR_WORKSPACE_NAME"} 